<p id="notice"><%= notice %></p>

<h1>Listing Habits</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Mativation</th>
      <th>Deadline</th>
      <th>Commit</th>
      <th>Declaration</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @habits.each do |habit| %>
      <tr>
        <td><%= habit.title %></td>
        <td><%= habit.mativation %></td>
        <td><%= habit.deadline %></td>
        <td><%= habit.commit %></td>
        <td><%= habit.declaration %></td>
        <td><%= link_to 'Show', habit %></td>
        <td><%= link_to 'Edit', edit_habit_path(habit) %></td>
        <td><%= link_to 'Destroy', habit, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Habit', new_habit_path %>
